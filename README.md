# Akatsuki(Open-sourced version)
System for ie-ryukyu which controls LDAP, Edy, IP, KVM, RADIUS.
This repository modified for open source(omit private informations(include spec)).

## Require environment
- ruby  2.3.0
- Rails 4.2.6
